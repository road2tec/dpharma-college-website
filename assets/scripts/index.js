// When the window is loaded, the modal will be displayed
window.onload = function () {
  console.log("Window loaded, showing modal");
  const modal = document.getElementById("banner_modal");
  modal.showModal();
};
